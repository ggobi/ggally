linters: with_defaults(
    camel_case_linter = NULL, # 922
    line_length_linter = NULL, # 237
    commented_code_linter = NULL, # 217
    infix_spaces_linter = NULL, # 108
    assignment_linter = NULL, # 53
    trailing_whitespace_linter = NULL, # 52
    spaces_left_parentheses_linter = NULL, # 33
    single_quotes_linter = NULL, # 25
    commas_linter = NULL, # 13
    object_usage_linter = NULL, # 10
    multiple_dots_linter = NULL, # 9
    trailing_blank_lines_linter = NULL, # 8
    NULL
  )
exclusions:
  list(
    "R/ggnetworkmap.R",
    "R/ggcorr.R",
    "R/ggsurv.R",
    "R/ggnet.R",
    "R/ggnet2.R"
  )
procedure:
  Remove a line from above. Each line is considered a "cancelation" of the linters.
  Fix all the errors.
code_to_lint_with:
  lintr::lint_package("./")
  # fix errors,
  # repeat until no errors
code_to_create:
  library(magrittr)
  library(dplyr)
  lintr::lint_package() %>%
    as.data.frame %>%
    group_by(linter) %>%
    tally(sort = TRUE) %$%
    sprintf("linters: with_defaults(\n    %s\n    NULL\n  )\n",
            paste0(linter, " = NULL, # ", n, collapse="\n    ")) %>%
    cat()
